import { PET_REMOTE_DESCRIPTORS } from './remote-schema.js'

export const TYPERT = { package: 'xy-deepseek-pet', face: 'host', schemas: [], invocations: PET_REMOTE_DESCRIPTORS, model: { services: [], events: [], objects: [] } }
