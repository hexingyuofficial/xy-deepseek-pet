import { SOUND_REMOTE_DESCRIPTORS } from './remote-schema.js'

export const TYPERT = {
  package: 'xy-deepseek-sounds',
  face: 'host',
  schemas: [],
  invocations: SOUND_REMOTE_DESCRIPTORS,
  model: { services: [], events: [], objects: [] },
}
