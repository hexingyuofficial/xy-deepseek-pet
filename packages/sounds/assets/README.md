# Placeholder sounds

These WAV files are generated by `../scripts/generate-placeholder-sounds.mjs` and released under CC0-1.0. They are intentionally plain temporary tones so final project audio can replace them without changing stable IDs.

`provenance.json` is regenerated from the shipped bytes and records each digest.
