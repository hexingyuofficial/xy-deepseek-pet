# Default sounds

These original 48 kHz, 24-bit PCM WAV files were provided by the project owner and are distributed under the repository's MIT license. Their stable internal IDs intentionally retain the `xy-placeholder-*` prefix so existing 0.1.0 user settings keep working after an upgrade.

`provenance.json` is regenerated from the shipped bytes and records each digest.
